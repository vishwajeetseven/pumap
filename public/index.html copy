<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>3D Map Effect</title>
  <style>
    html, body {
      margin: 0;
      padding: 0;
      overflow: hidden;
      width: 100%;
      height: 100%;
    }
    #map-container {
      position: relative;
      width: 100vw;
      height: 100vh;
      overflow: hidden;
    }
    #map {
      position: absolute;
      top: 50%;
      left: 50%;
      height: 100vh;
      width: auto;
      object-fit: cover;
      transform-origin: center center;
      transform: translate(-50%, -50%) scale(1);
      transition: transform 0.3s;
    }
    .controls {
      position: absolute;
      top: 10px;
      right: 10px;
      display: flex;
      flex-direction: column;
      background: rgba(255, 255, 255, 0.2);
      padding: 5px;
      border-radius: 10px;
      backdrop-filter: blur(10px);
      -webkit-backdrop-filter: blur(10px);
      box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
    }
    .controls button {
      font-size: 20px;
      margin: 2px;
      padding: 10px;
      border: none;
      cursor: pointer;
      background: rgba(255, 255, 255, 0.4);
      color: black;
      border-radius: 5px;
      width: 40px;
      height: 40px;
      text-align: center;
      backdrop-filter: blur(10px);
      -webkit-backdrop-filter: blur(10px);
      transition: background 0.3s;
    }
    .controls button:hover {
      background: rgba(255, 255, 255, 0.6);
    }
  </style>
</head>
<body>
  <div id="map-container">
    <img id="map" src="https://cdn.glitch.global/5bc9edf4-373c-47f7-9a1c-29acc401cd85/PU-Campus%20Map%202023%20latest.png?v=1742490596402" alt="Map">
    <div class="controls">
      <button id="zoom-in">+</button>
      <button id="zoom-out">-</button>
      <button id="rotate-map">↻</button>
    </div>
  </div>

  <script>
    const map = document.getElementById('map');
    let scale = 1;
    let rotation = 0; // Starting rotation angle (0 degrees)

    // Adjust initial transform properties based on screen size
    function updateTransform() {
      const transformValue = `translate(-50%, -50%) rotate(${rotation}deg) scale(${scale})`;
      map.style.transform = transformValue;
    }

    // Zoom in functionality
    document.getElementById('zoom-in').addEventListener('click', () => {
      scale += 0.1;
      updateTransform();
    });

    // Zoom out functionality
    document.getElementById('zoom-out').addEventListener('click', () => {
      scale = Math.max(0.5, scale - 0.1);
      updateTransform();
    });

    // Rotate map functionality (clockwise by 90 degrees with each click)
    document.getElementById('rotate-map').addEventListener('click', () => {
      rotation = (rotation + 90) % 360; // Rotate clockwise by 90 degrees
      updateTransform();
    });

    // Initial map transform
    updateTransform();
  </script>
</body>
</html>
